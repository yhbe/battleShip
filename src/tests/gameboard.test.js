import { Gameboard } from "../DOM/gameboard";

describe("testing gameboard", () => {
  let board;

  beforeEach(() => {
    board = new Gameboard();
  });

  test("Gameboard can place a ship at coordinates", () => {
    expect();
  });
});
